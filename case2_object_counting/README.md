# Caso 2 — Conteo Automático de Objetos

## Problema
[Describir el tipo de objetos a contar y el contexto de la imagen]

## Dataset
[Fuente utilizada — enlace de descarga y ubicación esperada en `data/raw/`]

## Pipeline
1. Preprocesamiento
2. Segmentación (`src/segmentation.py`)
3. Detección de contornos/componentes (`src/detection.py`)
4. Conteo (`src/counting.py`)
5. Evaluación de precisión (comparación contra conteo manual/ground truth)

## Resultados
Ver `results/figures/` y `results/metrics/`.

## Limitaciones
[Completar]
